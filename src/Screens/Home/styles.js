import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      padding : 10,
      paddingTop : 50,
      backgroundColor : "white",
      height : "100%"
    },
    directionRow : {
        display : "flex",
        flexDirection : "row"
    },
    logo : {
        fontWeight : 'bold',
        fontStyle : "italic"
    }

});